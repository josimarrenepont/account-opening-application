CREATE TABLE tb_amount (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   "value" DECIMAL,
   account_id BIGINT,
   CONSTRAINT pk_tb_amount PRIMARY KEY (id)
);

ALTER TABLE tb_amount ADD CONSTRAINT FK_TB_AMOUNT_ON_ACCOUNT FOREIGN KEY (account_id) REFERENCES tb_account (id);